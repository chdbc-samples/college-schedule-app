apiVersion: v1
kind: Service
metadata:
  name: {{ include "college-schedule.fullname" . }}
  labels:
    {{- include "college-schedule.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "college-schedule.selectorLabels" . | nindent 4 }}
